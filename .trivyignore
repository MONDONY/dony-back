# CVE-2026-22732: Spring Security - already patched in newer versions (6.5.9+)
# This project will upgrade during regular maintenance
CVE-2026-22732

# Stripe secret in application-dev.yml is a placeholder, not a real secret
# Real secrets are injected via GitHub Secrets at runtime
stripe-secret-token
