package com.dony.api.admin.account;

/**
 * Admin user status enum (Task 3).
 * - ACTIVE: User can log in and use the platform
 * - DISABLED: User account is disabled and cannot log in
 */
public enum AdminStatus {
    ACTIVE,
    DISABLED
}
