spring:
  datasource:
    url: jdbc:postgresql://localhost/placeholder   # overridden by @DynamicPropertySource (Testcontainers)
    username: placeholder
    password: placeholder
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

sentry:
  dsn: ""
  enabled: false

firebase:
  service-account-path: ""

hetzner:
  s3:
    endpoint: http://localhost:9000
    region: eu-central
    bucket: dony-test
    access-key: test
    secret-key: test

aws:
  s3:
    endpoint: http://localhost:9000
    region: eu-central
    bucket: dony-test
    access-key: test
    secret-key: test

stripe:
  secret-key: sk_test_dummy
  webhook-secret: whsec_dummy

dony:
  stripe:
    return-url: https://dony.app/payments/onboarding/return
    refresh-url: https://dony.app/payments/onboarding/refresh
  commission:
    rate: 0.12
